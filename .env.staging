# Railway preview deploy. No real secrets exist yet (skeleton backend
# has none) — fill in the actual Railway preview URL once provisioned.
API_BASE_URL=https://REPLACE-WITH-RAILWAY-PREVIEW-URL/api
ALLOWED_ORIGINS=https://staging.franciscoholdingsinc.com
ENVIRONMENT=staging
