name: Deploy Biotech Swarm Inc.

on:
  push:
    branches: [main, claude/francisco-revenue-sprint-MEva6]
    paths:
      - 'biotech-swarm-site/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout primedoxai-deploy
        uses: actions/checkout@v4

      - name: Clone or init biotech-swarm repo
        run: |
          git clone https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/franciscoderek7/biotech-swarm.git /tmp/biotech-swarm 2>/dev/null || {
            mkdir -p /tmp/biotech-swarm
            cd /tmp/biotech-swarm
            git init -b main
            git remote add origin https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/franciscoderek7/biotech-swarm.git
          }
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

      - name: Wipe and copy site files
        run: |
          find /tmp/biotech-swarm -mindepth 1 -not -path '/tmp/biotech-swarm/.git*' -delete 2>/dev/null || true
          cp -r biotech-swarm-site/. /tmp/biotech-swarm/
          touch /tmp/biotech-swarm/.nojekyll
          echo "Copied $(ls biotech-swarm-site/*.html 2>/dev/null | wc -l) HTML files"

      - name: Commit and push
        run: |
          cd /tmp/biotech-swarm
          git config user.email "deploy@franciscoholdingsinc.com"
          git config user.name "Empire Deploy Bot"
          git add -A
          git diff --cached --stat
          git commit -m "deploy: biotech-swarm @ ${{ github.sha }}" || echo "Nothing to commit"
          git push origin main || git push --set-upstream origin main
          echo "Live: franciscoderek7.github.io/biotech-swarm"
