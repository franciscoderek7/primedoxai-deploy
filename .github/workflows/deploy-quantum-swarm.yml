name: Deploy Quantum Swarm Inc.

on:
  push:
    branches: [main, claude/francisco-revenue-sprint-MEva6]
    paths:
      - 'quantum-swarm-site/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout primedoxai-deploy
        uses: actions/checkout@v4

      - name: Clone or init quantum-swarm repo
        run: |
          git clone https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/franciscoderek7/quantum-swarm.git /tmp/quantum-swarm 2>/dev/null || {
            mkdir -p /tmp/quantum-swarm
            cd /tmp/quantum-swarm
            git init -b main
            git remote add origin https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/franciscoderek7/quantum-swarm.git
          }
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

      - name: Wipe and copy site files
        run: |
          find /tmp/quantum-swarm -mindepth 1 -not -path '/tmp/quantum-swarm/.git*' -delete 2>/dev/null || true
          cp -r quantum-swarm-site/. /tmp/quantum-swarm/
          touch /tmp/quantum-swarm/.nojekyll
          echo "Copied $(ls quantum-swarm-site/*.html 2>/dev/null | wc -l) HTML files"

      - name: Commit and push
        run: |
          cd /tmp/quantum-swarm
          git config user.email "deploy@franciscoholdingsinc.com"
          git config user.name "Empire Deploy Bot"
          git add -A
          git diff --cached --stat
          git commit -m "deploy: quantum-swarm @ ${{ github.sha }}" || echo "Nothing to commit"
          git push origin main || git push --set-upstream origin main
          echo "Live: franciscoderek7.github.io/quantum-swarm"
