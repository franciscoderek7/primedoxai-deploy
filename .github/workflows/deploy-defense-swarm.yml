name: Deploy Defense Swarm Inc.

on:
  push:
    branches: [main, claude/francisco-revenue-sprint-MEva6]
    paths:
      - 'defense-swarm-site/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout primedoxai-deploy
        uses: actions/checkout@v4

      - name: Clone or init defense-swarm repo
        run: |
          git clone https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/franciscoderek7/defense-swarm.git /tmp/defense-swarm 2>/dev/null || {
            mkdir -p /tmp/defense-swarm
            cd /tmp/defense-swarm
            git init -b main
            git remote add origin https://x-access-token:${{ secrets.DEPLOY_TOKEN }}@github.com/franciscoderek7/defense-swarm.git
          }
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}

      - name: Wipe and copy site files
        run: |
          find /tmp/defense-swarm -mindepth 1 -not -path '/tmp/defense-swarm/.git*' -delete 2>/dev/null || true
          cp -r defense-swarm-site/. /tmp/defense-swarm/
          touch /tmp/defense-swarm/.nojekyll
          echo "Copied $(ls defense-swarm-site/*.html 2>/dev/null | wc -l) HTML files"

      - name: Commit and push
        run: |
          cd /tmp/defense-swarm
          git config user.email "deploy@franciscoholdingsinc.com"
          git config user.name "Empire Deploy Bot"
          git add -A
          git diff --cached --stat
          git commit -m "deploy: defense-swarm @ ${{ github.sha }}" || echo "Nothing to commit"
          git push origin main || git push --set-upstream origin main
          echo "Live: franciscoderek7.github.io/defense-swarm"
